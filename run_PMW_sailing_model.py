from PMW_sailing_model import *

main()

def main(rudder_angle = pi/10 , 
		 sail_angle = pi/2,
		 true_wind_polar = np.array([pi + (pi/6), 5]) )

for r in rudder_angles:
	for s in sail_angles:
		for t in true_wind_dirs:
			def main(rudder_angle = pi/10 , 
		 			 sail_angle = pi/2,
		 			 true_wind_polar = np.array([pi + (pi/6), 5]),
		 			 save_figs = False)
